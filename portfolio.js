// JavaScript for interactive elements and logging
document.addEventListener('DOMContentLoaded', () => {
    console.log('Personal portfolio loaded');
    // You can add interactive elements here if needed
});
